{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsMimoDomain",
  "title": "Mimo Domain Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the business domain.",
      "x-prompt": "What name would you like to use for the business domain?",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
