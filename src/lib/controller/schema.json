{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsMimoController",
  "title": "Mimo Controller Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the controller.",
      "x-prompt": "What name would you like to use for the controller?",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
