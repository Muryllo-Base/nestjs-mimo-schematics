{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsMimoConfig",
  "title": "Mimo Configuration Domain Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the Config Domain.",
      "x-prompt": "What name would you like to use for the Config Domain?",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
